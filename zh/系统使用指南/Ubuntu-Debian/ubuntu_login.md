# Ubuntu/Debian 登录用户和密码

默认登录账号：

- 用户：`firefly`
- 密码：`firefly`

Ubuntu Desktop 固件通常会自动登录到 `firefly` 桌面用户。需要管理员权限时使用 `sudo`：

```bash
sudo <command>
```

如需设置 root 密码，执行：

```bash
sudo passwd root
```
