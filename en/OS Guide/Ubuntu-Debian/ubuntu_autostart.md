# Ubuntu/Debian Application Autostart

Choose the startup method according to how the application should run:

- [Desktop autostart](ubuntu_autostart_desktop.md) for programs that start after a desktop user logs in.
- [systemd service autostart](ubuntu_autostart_systemd.md) for applications that run as background services.
